<%= form_for @news, url: url_into do |f| %>
	<ul>
		<% @news.errors.full_messages.each do |error| %>
			<i><%=error%></i>
		<% end %>
	</ul>
	<p>
		<%= f.label :title %><br/>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :body %><br/>
		<%= f.text_field :body %>
	</p>
	<p>
	  <%= f.label :tag_list %><br />
	  <%= f.text_field :tag_list %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>